const aiInputPopulate = async () => { try { const list = dropdownLists.find((i) => i.name === "category_list").listItems; if (list.length > 0) { const prompt = `A person has placed the following request: '${transcription}'.  Based on this request, select the best category this request matches from this list if categories: ${list}`; const gptResponse = await nlightnApi.askGPT(prompt); return gptResponse; } else { return; } } catch (error) { return; } }; aiInputPopulate();
